/**
 * Compatibility export for hosts that previously imported provider metadata
 * from the desktop application layer. New code should import from
 * `src/lib/model-providers`.
 */
export * from "../model-providers";
